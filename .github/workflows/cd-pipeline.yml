name: CD Pipeline

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Docker image tag to deploy'
        required: true

env:
  APP_NAME: comprehensive-production-ci-cd-pipeline-github-actions

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUBTOKEN }}
          fetch-depth: 0
          ref: git-actions-pipeline

      - name: Cleanup workspace
        run: git clean -fdx

      - name: Install Ansible
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible

      - name: Deploy Monitoring Stack & Print ArgoCD App Info
        run: |
          ansible-playbook Ansible/deploy_monitoring.yaml

      - name: Update Deployment Tag
        run: |
          echo "Updating argocd/deployment.yaml to use image_tag=${{ github.event.inputs.image_tag }}"
          IMAGE_TAG=${{ github.event.inputs.image_tag }}
          sed -i "s|\(image:.*${{ env.APP_NAME }}:\).*|\1${IMAGE_TAG}|g" argocd/deployment.yaml
          cat argocd/deployment.yaml

      - name: Commit & Push updated manifest
        id: commit_push
        run: |
          git config user.name "mohamedesmael10"
          git config user.email "mohamed.2714104@gmail.com"

          if git diff --quiet argocd/deployment.yaml; then
            echo "No changes detected in deployment.yaml. Skipping commit and deploy."
            echo "changed=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          git add argocd/deployment.yaml
          git commit -m "Update Deployment Manifest image_tag=${{ github.event.inputs.image_tag }}"
          git push origin HEAD:git-actions-pipeline
          echo "changed=true" >> $GITHUB_OUTPUT

      - name: Notify on Success
        if: ${{ success() }}
        run: echo "CD Pipeline completed successfully for image_tag=${{ github.event.inputs.image_tag }}"

      - name: Notify on Failure
        if: ${{ failure() }}
        run: echo "CD Pipeline failed. Check logs for details."

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: mohamed.esmael.jenkins@gmail.com
          password: ${{ secrets.SMTPPASSWORD }}
          subject: |
            CD Pipeline Status: ${{ job.status }}
          to: mohamed.2714104@gmail.com
          from: mohamed.esmael.jenkins@gmail.com
          body: |
            Hello,

            The CD Pipeline has completed.

            Status: ${{ job.status }}
            Image tag deployed: ${{ github.event.inputs.image_tag }}

            Repository: ${{ github.repository }}
            Workflow run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Regards,
            CI/CD Bot
